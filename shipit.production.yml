deploy:
  override:
    - yarn install
    - yarn build && yarn changeset publish
  variables:
    - name: npm_config_registry
      title: "where to get the npm info from"
      default: "https://registry.npmjs.org"
