import { expect, describe, it } from 'vitest';
import { toSourceCode } from './index';

describe('Module: toSourceCode', () => {
  it('should exist', () => {
    expect(toSourceCode).to.exist;
  });
});
