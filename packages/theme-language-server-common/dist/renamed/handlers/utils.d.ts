import { Template } from '@shopify/theme-check-common';
export declare function isValidTemplate(parsed: unknown): parsed is Template.Template;
export declare function isValidSectionGroup(parsed: unknown): parsed is Template.SectionGroup;
