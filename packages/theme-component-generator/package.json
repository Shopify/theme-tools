{
  "name": "theme-component-generator",
  "version": "1.0.0",
  "description": "MCP (Model Context Protocol) server to generate basic Liquid components",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "start": "node --experimental-strip-types src/index.ts",
    "dev": "node --experimental-strip-types --watch src/index.ts",
    "build": "echo noop",
    "build:ts": "echo noop",
    "build:ci": "echo noop",
    "postbuild:ts": "echo noop",
    "generate-factory-configs": "echo noop",
    "type-check": "echo noop",
    "test": "npx promptfoo eval -c tests/mcp.yaml"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "liquid",
    "theme",
    "component",
    "generator",
    "server",
    "typescript"
  ],
  "author": "Alok Swamy <alok.swamy@shopify.com>",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.13.1",
    "zod": "^3.25.32"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  },
  "engines": {
    "node": ">=18.20.8"
  }
}