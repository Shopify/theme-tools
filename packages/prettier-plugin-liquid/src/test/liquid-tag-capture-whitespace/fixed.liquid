It should pretty print the contents of a capture tag with captureWhitespace ignore
printWidth: 1
captureWhitespace: "ignore"
{% capture _ %}
  {% for element in col %}
    {{ element }}
  {% endfor %}
{% endcapture %}  

It should print as is when strict
printWidth: 1
captureWhitespace: "strict"
{% capture _ %}
{%for element in col%}
{{element}}
{%endfor%}
{% endcapture %}