When there's a region collapsed comment, should not surround with new lines
<div>
  {% # region(collapsed) %}
  <link rel="{{ 'title.css' | asset_url }}" rel="stylesheet">
  {% # endregion %}
</div>

