import { Position } from '../types';
export declare function getPosition(source: string, index: number): Position;
export declare function getOffset(source: string, line: number, column: number): number;
